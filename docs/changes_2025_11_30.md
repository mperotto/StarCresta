# Mudanças - 30/11/2025

## Correções Visuais no Minigame 3D (Planet Stage)

- **Ocultação da Nave Externa**: Modificado `src/core/game.py` para não desenhar o sprite do jogador (`self.jogador.desenhar`) quando o jogo estiver no modo `in_planet_stage`. Isso garante que apenas a visão interna do cockpit seja apresentada.
- **Ocultação de Elementos 2D**: Também foram ocultados upgrades, destroços, tiros e inimigos durante o estágio do planeta para manter a imersão da visão em primeira pessoa.
- **Máscaras de Janela**: Verificado que `src/core/planet_stage.py` já implementa máscaras (`_blit_viewfinders`) para limitar a visão 3D às janelas do cockpit, conforme solicitado.

## Arquivos Modificados
- `src/core/game.py`: Adicionada condição `if not self.in_planet_stage:` ao redor das chamadas de desenho de entidades 2D.
